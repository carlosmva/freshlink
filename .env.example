# Local / production secrets — never commit the real values.

# Local/dev AI (Cloudflare Workers AI). Not used on the live Netlify site.
API_TOKEN=your_cloudflare_api_token
CLOUDFLARE_URL=https://api.cloudflare.com/client/v4/accounts/YOUR_ACCOUNT_ID/ai/run/@cf/openai/gpt-oss-120b

# Live site AI (Claude). Set this in Netlify production env.
# CLAUDE_ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL_ID=claude-haiku-4-5-20251001

JWT_SECRET=change-me
DATABASE_URL=postgresql://user:pass@ep-xxx.neon.tech/neondb?sslmode=require
